[package]
name = "openclaw-runtime"
version = "1.0.0"
edition = "2021"
build = "build.rs"

[dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "macros", "sync", "time"] }

[build-dependencies]
cmake = "0.1"

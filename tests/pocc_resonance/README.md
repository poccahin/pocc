# 🌍 Life++ Resonance Testnet (The One-Click Planetary Sandbox)

Welcome to the deterministic testnet of the Life++ Protocol. This sandbox proves that our mathematical constraints (Tensor Wind Tunnels, CR+ Routing, and Edge Terminal Netting) work in real-time across polyglot microservices.

## 🚀 Ignition

Ensure you have Docker and `docker-compose` installed, then run:

```bash
docker-compose up --build
```

## 👁️ What You Are Witnessing

Once the containers spin up, open your browser to **http://localhost:3000**. You are now looking at the **Koala OS Omnisphere**.

- **🟦 Blue Pulses:** These represent successful AP2 intents and Daily Netting (Zha-Zhang) transactions from edge computing terminals globally. The Python Validator approved the tensor, and the Rust Router achieved physical consensus.
- **🟥 Massive Red Pulses:** Every few seconds, the `swarm_simulator` injects a highly volatile, adversarial "Poisoned Tensor" simulating a Cartel attack. Watch as the PyTorch L2.5 firewall instantly detects the semantic drift variance and triggers a **Reputation Slash**—visually represented by a massive red thermal explosion on the map.

**This is not an animation.** Every pulse you see is a real, synchronous TCP request passing through a Rust asynchronous daemon, hitting a Python Monte Carlo validation engine, and broadcasting via WebSockets to WebGL.

*Engineering Value Alignment and Responsibility Traceability, one tensor at a time.*
